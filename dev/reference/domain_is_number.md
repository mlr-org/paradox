# Whether a Given Domain is Numeric

This should generally be `TRUE` for discrete or continuous numeric
[`Domain`](https://paradox.mlr-org.com/dev/reference/Domain.md)s, and
`FALSE` otherwise.

## Usage

``` r
domain_is_number(param)
```

## Arguments

- param:

  (`Domain`).

## Value

`logical`.
