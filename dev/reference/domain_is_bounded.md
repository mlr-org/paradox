# Whether a Given Domain is Bounded

This should generally be `TRUE` when `lower` and `upper` are given and
finite, or when the `nlevels` is finite, and `FALSE` otherwise.

## Usage

``` r
domain_is_bounded(param)
```

## Arguments

- param:

  (`Domain`).

## Value

`logical`.
