#' @field storage_type (`character(1)`)\cr
#' Data type when values of this parameter are stored in a data table or sampled.
#' Always `"numeric"` for [`p_dbl()`].
#' Always `"character"` for [`p_fct()`].
#' Always `"integer"` for [`p_int()`].
#' Always `"logical"` for [`p_lgl()`].
#' Always `"list"` for [`p_uty()`].
