#' @param tolerance (`numeric(1)`)\cr
#'   Initializes the `$tolerance` field that determines the tolerance of the
#'   `lower` and `upper` values. Must be finite and non-negative; for
#'   `p_int()` it must additionally not exceed `0.5`.
