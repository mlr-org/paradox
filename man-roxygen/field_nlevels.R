#' @field nlevels (`integer(1)` | `Inf`)\cr
#'   Number of categorical levels.
#'   Always `Inf` for [ParamDbl] and [ParamUty].
#'   The number of integers in the range `[lower, upper]`, or `Inf` if unbounded for [ParamInt].
#'   Always `2` for [ParamLgl].
