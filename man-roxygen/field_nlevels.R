#' @field nlevels (`integer(1)` | `Inf`)\cr
#' Number of categorical levels.
#' Always `Inf` for [`p_dbl()`] and [`p_uty()`].
#' The number of integers in the range `[lower, upper]`, or `Inf` if unbounded for [`p_int()`].
#' Always `2` for [`p_lgl()`].
